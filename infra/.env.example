# Docker-compose overrides — copy to infra/.env and fill in for production.
# In local development the defaults (nutrisync/nutrisync) are fine.
POSTGRES_USER=nutrisync
POSTGRES_PASSWORD=nutrisync
POSTGRES_DB=nutrisync
